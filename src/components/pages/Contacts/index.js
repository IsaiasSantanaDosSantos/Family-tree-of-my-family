import { ContactsContainer } from './styles';

const Contacts = () => {
  <ContactsContainer>
    <section>
      <h1>Contacts page</h1>
    </section>
    ;
  </ContactsContainer>;
};

export default Contacts;
